//A simple class called Recipe to handle simple Recipes, right now the model of favorites (will change in future probably)

class Recipe {
  final String imagePath;
  final String title;

  Recipe(this.imagePath, this.title);
}